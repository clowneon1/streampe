<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#12151f"
    android:layout_marginBottom="8dp"
    android:padding="16dp">

    <!-- Row 1: time · app · TEST badge -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="10dp">

        <TextView
            android:id="@+id/tvLogTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textColor="#555D7A"
            android:layout_marginEnd="8dp" />

        <TextView
            android:id="@+id/tvLogApp"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="#8890AA"
            android:ellipsize="end"
            android:maxLines="1" />

        <!-- TEST badge -->
        <TextView
            android:id="@+id/tvTestBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=" SIM "
            android:textSize="9sp"
            android:textColor="#000000"
            android:textStyle="bold"
            android:background="#FBBF24"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Row 2: Sender + Amount -->
    <LinearLayout
        android:id="@+id/layoutChips"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="12dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/tvLogSender"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="14sp"
            android:textColor="#F0F2FF"
            android:ellipsize="end"
            android:maxLines="1"
            android:layout_marginEnd="12dp" />

        <TextView
            android:id="@+id/tvLogAmount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#6C63FF" />

    </LinearLayout>

    <!-- Row 3: notification text -->
    <TextView
        android:id="@+id/tvLogText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#8890AA"
        android:maxLines="3"
        android:ellipsize="end"
        android:layout_marginBottom="16dp" />

    <!-- Row 4: hidden title (kept for compat, not shown) -->
    <TextView
        android:id="@+id/tvLogTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone" />

    <!-- Row 5: Retrigger + Delete buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnRetrigger"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            style="@style/Widget.App.Button"
            android:text="↻  Retrigger"
            android:textSize="12sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#6C63FF"
            android:layout_marginEnd="8dp"
            android:stateListAnimator="@null" />

        <Button
            android:id="@+id/btnDeleteEntry"
            android:layout_width="40dp"
            android:layout_height="40dp"
            style="@style/Widget.App.Button"
            android:text="✕"
            android:textSize="14sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#2e3248"
            android:stateListAnimator="@null" />

    </LinearLayout>

</LinearLayout>
