{
  "name": "payment-alerts-obs-server",
  "version": "2.0.0",
  "description": "Payment Alerts for OBS - PC Server App",
  "author": "clowneon1",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch server.js server.js",
    "bun:build": "node scripts/build-bun-sidecar.js",
    "app:dev": "node scripts/build-bun-sidecar.js && tauri dev",
    "app:dist": "node scripts/package-release.js",
    "version": "node scripts/sync-version.js && git add src-tauri/Cargo.toml src-tauri/tauri.conf.json"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "express": "^4.18.2",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "nodemon": "^3.0.1"
  },
  "pkg": {
    "assets": [
      "public/**/*"
    ],
    "scripts": [
      "server.js"
    ]
  }
}
